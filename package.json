{
  "name": "outra-directmail",
  "version": "0.1.0",
  "private": true,
  "description": "Outra Direct Mail subscription funnel — postcode selection → DM design → Stripe Checkout.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod --yes"
  },
  "dependencies": {
    "@vercel/blob": "^0.23.4",
    "stripe": "^14.25.0"
  }
}
